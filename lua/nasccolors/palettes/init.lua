local M = {}

--- Returns a color palette based on a ColorVariant.
--- @param variant ColorVariant
M.get_palette = function(variant)
  -- print usage to check if variant thing works
  vim.print('let\'s get the ' .. variant .. ' color palette...')
end

return M
